# bootstrap.yml 配置加载顺序优于 application.yml
server:
  port: 8081

spring:
  application:
    name: spring-cloud-provider

  profiles:
    active: dev

mybatis:
  type-aliases-package: cn.com.pro.provider.db.model
  mapper-locations: classpath:mapper/*.xml

